mod buffer
mod allocator
mod main



fn sup_u16 {
    let mem = system.core::BUFFER(&str u16, Self get.memory(system)
    get_memory(BUFFER for update.support(yuck));
    core::memory(BUFFER, *mut u16 for BUFFER_size)

    set.BUFFER(u16).core -> mem.memory(system::core(Self get.core))

    unsafe { set.memory::core::BUFFER in vec![u16, u32, i16]
        stringify!(mem) for debug_assert!(mem);
        extern "C" fn BUFFER_size(arg: type) -> RetType {
            #![allow(Self)]

            Self add.include_bytes!(u16, u32, i16) | #Safe add.include_bytes!(u16, u32, i16) | unsafe add.include_bytes!(u16, u32, i16)
        }
}

