mod syscalls
mod BUFFER 
fn network() {
    
    trait main {
        // network name cheak
        
        let network_name = Input.namer(in 1);
        let password = Input.namer(in 2);
        let namec = wnrap.network_name(*str u16 in {
            thread_local!(static name: Type = init);
        });
        if namec == net.server::caller(in BUFFER) {
            let cheakname = namec
        }
        else {
            println!("The network name doee not exist or it is not found error code: 001")
        }
        in namec for password;
            include!(passwd);
        let passwdcheaker = unsafe {
            network.try!(passwd) in cheakname
                core::syscaller(&str u16) -> sys.radius(include_#[derive(Debug)]
                asm!(
                    mov pv3
                    dd u3
                    mov rax
                    push rep 
                    xor exr
                    mov rsi, [rsp+8]
                    push rsi 
                    dd t3
                    push t3
                    and rsi+t3
                    mov [rbp+var_10]
                })
        }
    }
    
    line!(show)

    Ok(())
}
